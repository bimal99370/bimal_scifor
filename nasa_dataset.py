# -*- coding: utf-8 -*-
"""NASA DATASET.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xi0afd2JSwPjOIKVB6hkIG1qG0dgGOSp
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

df = pd.read_csv('/content/nasa_aug95_c.csv')

df.head(50)

df.groupby('datetime').size().plot()

df.shape

df.describe()

df.isnull().sum()

df.dropna()

fig = plt.figure(figsize = (10,6))

corr=df.corr()
sns.heatmap(corr,annot=True)

sns.barplot(x="requesting_host",y="status",data=df)

column_to_plot = 'status'

plt.hist(df['status'], bins=30, color='blue', edgecolor='black')
plt.title('Histogram of status')
plt.xlabel('status')
plt.ylabel('datetime')
plt.show()

plt.hist(df['status'], bins=30, color='blue', edgecolor='black')
plt.title('Histogram of status')
plt.xlabel('status')
plt.ylabel('datetime')
plt.show()

mean_response_size = np.mean(df['response_size'])
max_response_size = np.max(df['response_size'])

print("Mean response size  " ,mean_response_size )
print("Max response size: ",max_response_size )

X_test['response_size'] = pd.to_datetime(X_test['response_size'])

X_test['response_size'] = X_test['response_size'].dt.response_size

y_pred = model.predict(X_test)

plt.figure(figsize=(8, 6))
plt.scatter(y_test, y_pred, color='blue', label='response_size')
plt.plot([min(y_test), max(y_test)], [min(y_test), max(y_test)], color='red', linestyle='--', label='Perfect response_size')
plt.xlabel('response_size')
plt.ylabel('response_size')
plt.title('request vs response_size')
plt.legend()
plt.show()

